specVersion: "v1"
serviceId: "stressbuster"
location: "asia-southeast1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "stressbuster-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
